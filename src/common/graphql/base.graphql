scalar DateTime

interface BaseNode {
  id: ID!
  createTime: DateTime
  updateTime: DateTime
}

interface BaseList {
  count: Float
  page: Float
  pageSize: Float
  timestamp: Float
}

interface BaseCountData {
  count: Float
}

type Status {
  done: Boolean
}
